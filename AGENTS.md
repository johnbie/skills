# AGENTS.md

Guidance for CLI coding agents working in this repo. This is a collection of plain-Markdown Agent Skills; the work here is editing `SKILL.md`/`README.md` files and shell, committed and shipped via `git`/`gh`.

## Requirements

### Skills
- `repo-doctor` — confirm the toolchain and `git`/`gh` are ready before working here (source: github.com/johnbie/skills)
- `agent-doctor` — this repo defines and assumes it; run it against this `## Requirements` section (source: github.com/johnbie/skills)
